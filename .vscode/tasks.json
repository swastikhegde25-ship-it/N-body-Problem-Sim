{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Physics DLL",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-Command",
                "if (!(Test-Path build)) { New-Item -ItemType Directory -Path build }; cl /nologo /O2 /openmp /W4 /LD /I c_core/include c_core/src/*.c /Fo:build/ /Fe:build/physics.dll"
            ],
            "group": { "kind": "build", "isDefault": true },
            "problemMatcher": ["$msCompile"],
            "detail": "Creates build folder and compiles modular source into physics.dll"
        }
    ]
}